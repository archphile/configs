# http://andrews-corner.org/abcde.html

#FLACENCODERSYNTAX=flac
#FLACOPTS='--verify --best'
#OUTPUTTYPE="flac"

MP3ENCODERSYNTAX=lame
LAME=lame
LAMEOPTS='-V 2'
OUTPUTTYPE="mp3"

#AACENCODERSYNTAX=faac
#AACENC=faac
#AACENCOPTS='-q 250 -w -s'
#OUTPUTTYPE="m4a

CDROMREADERSYNTAX=cdparanoia
CDPARANOIA=cdparanoia
CDDISCID=cd-discid

WAVOUTPUTDIR="/tmp/WORK" # standard RAM disc which by default uses 1/2 of your physical memory
OUTPUTDIR="/media/data/New_CD/Audio/" # where the finished products go
ACTIONS=cddb,read,encode,tag,move,clean
OUTPUTFORMAT='${OUTPUT}/${ARTISTFILE}-${ALBUMFILE}/${TRACKNUM}_${TRACKFILE}'
VAOUTPUTFORMAT='${OUTPUT}/VA-${ALBUMFILE}/${TRACKNUM}_${ARTISTFILE}-${TRACKFILE}'

mungefilename ()
{
  echo "$@" | sed s,:,-,g | tr / _ | tr -d \'\"\?\[:cntrl:\]
}

MAXPROCS=4	# set to number of cores
PADTRACKS=y
EXTRAVERBOSE=y
EJECTCD=y
