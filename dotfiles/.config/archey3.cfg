[core]
color = blue
align = center
display_modules = distro(), uname(n), uname(r), uptime(), wm(), de(), packages(), ram(), fs(/), fs(/home), fs(/boot), fs(/var), fs(/media/data)

[fs]
low_bound = 75
medium_bound = 90
